- [x] Publish non game 
- [] Random GUI (single)
- [] Responsive & component tree (same layers) [new plot]
- [] Codi python que dibuixa a partir SVG (+ interactiveMode (calibrate) + svgMode layers (plot))

- [] Genuary interesting (boolean)